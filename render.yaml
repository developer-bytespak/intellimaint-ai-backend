services:
  - type: web
    name: intellimaint-gateway
    env: node
    region: singapore
    plan: free
    rootDir: gateway
    buildCommand: npm install && npm run build
    startCommand: npm run start:prod
    envVars:
      - key: NODE_ENV
        value: production